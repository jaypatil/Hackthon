.login-form-wrap {
    .make-row();
}

.login-create-account {
    .make-sm-column(5);
    .make-sm-column-offset(1);

    .btn-create-account {
        .button-variant(@white; @brand-secondary; @brand-secondary);
    }

    .create-account-button {
        border-top: dashed 1px @gray-lighter;
        margin-top: @padding-base-vertical * 2;
        padding-top: @padding-base-vertical * 2;
        text-align: center;
    }
}

.login-form {
    .make-sm-column(4);
    .make-sm-column-offset(1);

    .btn-login {
        padding-left: @padding-base-horizontal * 2;
        padding-right: @padding-base-horizontal * 2;
    }

    .button-group .control-field,
    .remember-group .control-field {
        .make-md-column-offset(3);
    }

    .control-field {
        .make-md-column(9);
    }

    .control-label {
        .make-md-column(3);
    }

    .forgot-password p {
        .make-md-column(9);
        .make-md-column-offset(3);
        margin: 0;
    }

    .remember-group {
        .checkbox {
            padding-top: 0;
        }
    }
}

.login-logged-in {
    .make-sm-column(4);
    .make-sm-column-offset(4);
}
